import 'package:flutter/material.dart';

class AppColors {
  // --- Modern Indigo Palette (Premium & Elegant) ---
  static const Color primary = Color(0xFF4F46E5); // Indigo 600
  static const Color secondary = Color(0xFF6366F1); // Indigo 500
  static const Color accent = Color(0xFFEEF2FF); // Indigo 50
  
  static const Color backgroundColor = Color(0xFFF5F7FF); // Indigo-Tinted White
  static const Color textDark = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);

  // --- Hero Section Gradient (Modern Indigo) ---
  static const List<Color> heroGradient = [
    Color(0xFFEEF2FF), // Indigo 50
    Color(0xFFE0E7FF), // Indigo 100
    Color(0xFFC7D2FE), // Indigo 200
  ];

  static const Color heroText = Color(0xFF312E81); // Indigo 900
  static const Color heroBadge = Color(0xFFE0E7FF);
  static const Color heroBadgeText = Color(0xFF4338CA); // Indigo 700
}
